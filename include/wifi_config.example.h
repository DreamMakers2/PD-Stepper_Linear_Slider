#pragma once

// Copy this file to include/wifi_config.h and edit the values there.
// include/wifi_config.h is intentionally ignored by Git.

#define SLIDER_WIFI_SSID "your-wifi-name"
#define SLIDER_WIFI_PASSWORD "your-wifi-password"

// Used only if the station connection fails. WPA2 requires at least 8 chars.
#define SLIDER_FALLBACK_AP_PASSWORD "change-this-password"
